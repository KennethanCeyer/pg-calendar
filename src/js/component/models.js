define([], () => {
    const model = {
        ComponentName: 'pignoseCalendar',
        ComponentVersion: '1.4.20',
        ComponentPreference: {
            supports: {
                themes: ['light', 'dark', 'blue']
            }
        }
    };
    return model;
});
